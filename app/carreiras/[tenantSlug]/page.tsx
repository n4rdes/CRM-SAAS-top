/* eslint-disable @next/next/no-img-element */
import Link from "next/link"; import {notFound} from "next/navigation"; import {createClient} from "@/lib/supabase/server"; import "../careers.css";
type Career={tenant:{name:string;slug:string;title:string;description:string;primary_color:string;logo_url?:string};jobs:Array<{id:string;title:string;description?:string;employment_type:string;workplace_type:string;city?:string;state?:string;salary_min_cents?:number;salary_max_cents?:number}>};
export default async function CareerPage({params}:{params:Promise<{tenantSlug:string}>}){const {tenantSlug}=await params;const supabase=await createClient();const {data}=await supabase.rpc("get_public_career_page",{p_tenant_slug:tenantSlug});const career=data as Career|null;if(!career)notFound();return <main className="career-site" style={{"--career-color":career.tenant.primary_color} as React.CSSProperties}><header><div>{career.tenant.logo_url?<img src={career.tenant.logo_url} alt={career.tenant.name}/>:<span>{career.tenant.name[0]}</span>}<strong>{career.tenant.name}</strong></div><small>Oportunidades powered by Prismae</small></header><section className="career-hero"><small>CARREIRAS</small><h1>{career.tenant.title}</h1><p>{career.tenant.description}</p><a href="#vagas">Ver oportunidades ↓</a></section><section id="vagas" className="career-jobs"><div><small>OPORTUNIDADES ABERTAS</small><h2>Encontre seu próximo desafio</h2></div>{career.jobs.length?<div>{career.jobs.map(job=><Link href={`/carreiras/${tenantSlug}/vagas/${job.id}`} key={job.id}><div><span>{job.workplace_type}</span><h3>{job.title}</h3><p>{[job.city,job.state,job.employment_type].filter(Boolean).join(" · ")}</p></div><b>Ver vaga →</b></Link>)}</div>:<article>Nenhuma oportunidade aberta neste momento.</article>}</section><footer>{career.tenant.name} · Processo seletivo seguro e transparente</footer></main>}
